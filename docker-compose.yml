services:
  app:
    build: .
    container_name: memoreel-backend
    environment:
      SPRING_PROFILES_ACTIVE: ${SPRING_PROFILES_ACTIVE:-prod}
      SERVER_PORT: ${SERVER_PORT:-8080}
      DB_URL: ${DB_URL}
      DB_USERNAME: ${DB_USERNAME}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_DRIVER: ${DB_DRIVER:-com.mysql.cj.jdbc.Driver}
      JPA_DDL_AUTO: ${JPA_DDL_AUTO:-update}
      JPA_SHOW_SQL: ${JPA_SHOW_SQL:-false}
      MEMOREEL_STORAGE_S3_BUCKET: ${NETZERO3_STORAGE_S3_BUCKET:netzero-03-s3}
      MEMOREEL_STORAGE_S3_REGION: ${NETZERO3_STORAGE_S3_REGION:us-east-1}
    ports:
      - "${SERVER_PORT:-8080}:${SERVER_PORT:-8080}"
    restart: unless-stopped
